---
import PageLayout from "@/layouts/Base";
import { Image } from "@astrojs/image/components";
import aboutImg from "../assets/about-astro.png";

const meta = {
	title: "Oops! You found a missing page!",
	description: "It looks like this page is lost in space!",
};
---

<PageLayout meta={meta}>
	<h1 class="title mb-6">{meta.title}</h1>
	<p class="mb-8">{meta.description}</p>
	<div class="my-4 grid justify-center">
		<Image
			class="rotate-180"
			src={aboutImg}
			alt="A cartoon cactus looking at the 'Astro.build' logo"
			loading="eager"
		/>
	</div>
</PageLayout>
